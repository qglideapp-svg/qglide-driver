name: driver
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.16+23

environment:
  sdk: ^3.12.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  video_player: ^2.11.1
  flutter_svg: ^2.3.0
  google_maps_flutter: ^2.17.1
  geolocator: ^14.0.2
  http: ^1.6.0
  image: ^4.9.1
  image_picker: ^1.0.4
  image_picker_android: ^0.8.13
  shared_preferences: ^2.5.4
  url_launcher: ^6.3.2
  flutter_riverpod: ^3.3.2
  geocoding: ^4.0.0
  webview_flutter: ^4.14.0
  uuid: ^4.5.3
  permission_handler: ^11.3.1
  google_sign_in: ^6.3.0
  sign_in_with_apple: ^6.1.4
  crypto: ^3.0.3
  zego_express_engine: ^3.23.0
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1
  device_info_plus: ^11.2.0
  audioplayers: ^6.6.0
  supabase: ^2.13.0
  share_plus: ^12.0.1
  flutter_foreground_task: ^10.0.0
  wakelock_plus: ^1.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  image: ^4.9.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  config:
    enable-swift-package-manager: false

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/anime.mp4
    - assets/splash_poster.png
    - assets/app_logo.png
    - assets/flags/qatar_flag.png
    - assets/first_screen.png
    - assets/second_screen.png
    - assets/third_screen.png
    - assets/fouth_screen.png
    - assets/Background images.png
    - assets/Background images (1).png
    - assets/olicks 1.png
    - assets/success.gif
    - assets/72129__kizilsungur__sweetalertsound5.wav
    - assets/527663__iwanplays__blinking-bell-loop.wav
    - assets/Subtract.png
    - assets/Rectangle 240647757.png
    - assets/icons/car-01.png
    - assets/icons/driver_car.png
    - assets/icons/Frame.png
    - assets/icons/Vector.png
    - assets/icons/add-circle.png
    - assets/icons/reverse-withdrawal-01.png
    - assets/icons/wallet-05.png
    - assets/icons/wallet-done-02.png
    - assets/icons/invoice-01.png
    - assets/icons/wallet-remove-01.png
    - assets/icons/Vector (1).png
    - assets/icons/user-circle.png
    - assets/profile_photo.png
    - assets/manage_vehicle_photo.png
    - assets/google_logo.svg
    - shorebird.yaml

  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: "fonts/DM_Mono,Plus_Jakarta_Sans/Plus_Jakarta_Sans/static/PlusJakartaSans-Regular.ttf"
          weight: 400
        - asset: "fonts/DM_Mono,Plus_Jakarta_Sans/Plus_Jakarta_Sans/static/PlusJakartaSans-Bold.ttf"
          weight: 700
    - family: Satoshi
      fonts:
        - asset: fonts/satoshi/Satoshi-Regular.otf
          weight: 400
        - asset: fonts/satoshi/Satoshi-Medium.otf
          weight: 500
        - asset: fonts/satoshi/Satoshi-Bold.otf
          weight: 700
        - asset: fonts/satoshi/Satoshi-Black.otf
          weight: 900

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  macos:
    generate: true
  web:
    generate: true
  windows:
    generate: true
  image_path: "assets/app_logo.png"
  remove_alpha_ios: true
  background_color_ios: "#000000"
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/app_logo.png"
